#pragma strict

function Start () {

}

function Update () {
	//how the fuck do i make things rotate.
	//transform.RotateAround (GetComponent(MeshRenderer).bounds.center, Vector3.up, 20 * Time.deltaTime);
}